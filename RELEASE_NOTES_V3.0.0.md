# 🚀 ST-Direct-TTS V3.0.0 全量发布更新日志 (Release Notes)

> **版本号**: `v3.0.0` (旗舰重构大版本)  
> **发布日期**: 2026-08-22  
> **核心定位**: 🎨 创作者工坊 (Creative Workshop) · 🌌 巅峰高定主题生态 · 🧠 深度提示词与上下文注入 · ⚡ 全链路非阻塞异步队列 · 🎙️ 多 TTS 供应商策略架构

---

## 🌟 重磅更新概览 (Highlights)

V3.0.0 是 ST-Direct-TTS 自诞生以来**规模最大、架构最纯净、视觉与交互最震撼**的里程碑大版本。我们彻底打破了传统固定场景与单一外观的束缚，带来创作者工坊、四大巅峰主题、全链路非阻塞异步调度队列以及多供应商扩展架构。

```
                                  ST-Direct-TTS V3.0.0 架构全景
  ┌─────────────────────────────────────────────────────────────────────────────────────────┐
  │                               SillyTavern 原生前端接入层                                 │
  ├───────────────────┬────────────────────────────┬────────────────────┬───────────────────┤
  │   🎨 创作者工坊   │    🌌 高定主题系统 2.0     │  ⚡ 全异步调度引擎 │  🎙️ 多供应商策略  │
  │  • 纯 JSON 预设   │  • ⚔️ 仙途凌霄 (仙侠水墨)  │  • 待听多任务排队  │  • 本地 GPT-SoVITS│
  │  • 提示词自由定制 │  • 🌸 落樱雅境 (平安京)    │  • 后台静默生成    │  • MiniMax API    │
  │  • 社区一键导入出 │  • ⚡ 边缘行者 (赛博朋克)  │  • 分段字幕同步    │  • 豆包/火山引擎  │
  │  • 角色/变量插槽  │  • 🪄 死亡圣器 (哈利波特)  │  • 铭刻聊天记录    │  • 动态端口/反代  │
  └───────────────────┴────────────────────────────┴────────────────────┴───────────────────┘
```

---

## 📋 核心更新项目详细清单

### 1. 🎨 创作者工坊生态 (Creative Workshop)
* **纯 JSON 预设架构**：支持用户自由定义和编排任意剧情场景，包括场景分类（`phone_call` 主动来电 / `eavesdrop` 私下窃听）、诱发原因、角色动机、情绪基调与 BGM。
* **智能变量插槽系统**：在编写 System Prompt 时提供一键插槽注入（`{{speaker}}`、`{{user_name}}`、`{{speakers_emotions}}`、`{{context}}`、`{{lang_display}}`、`{{last_call_summary}}`、`{{followup_call_instructions}}` 等）。
* **AI 剧本生成设想**：内置 AI 剧本生成助手，仅需输入剧情一句话设想，即可自动生成完整结构化预设。
* **一键导入与导出**：预设支持单文件 `.json` 导出与导入，方便创作者在社区、Q群中自由分享优质剧情预设。
* **前端工坊独立 App 模块化**：拆分为 `api.js`、`edit_modal.js`、`directed_modal.js`、`import_modal.js`、`executor.js`、`templates.js`、`svgs.js`，弹窗自适应任意屏幕。

---

### 2. 🌌 巅峰视觉高定主题系统 (Theme Engine 2.0)
* **四大官方旗舰预置主题实装**：
  1. **⚔️ 仙途凌霄 (`immortal_sword`)**：
     * **灵气交互**：先天太极双鱼八卦盘悬浮球（常态慢转、悬停加速、来电极速旋转伴随金色灵气波）；
     * **艺术排版**：天心剑脉中轴光轨 + 左右交错悬浮浮岛灵牒 + 八角工笔玉印徽记；
     * **粒子物理引擎**：青玉螺旋剑气、玄青水墨流云微粒与灵金光尘 Canvas 物理模拟；
     * **场景美学**：中式飞檐如意角花模态框、仙门水墨宣纸长卷，100% 矢量 SVG 图元化。
  2. **🌸 平安京·落樱雅境 (`sakura_elegance`)**：
     * **风雅交互**：3D 悬浮落樱折扇与晴明结界五芒星流光印；
     * **色盘升级**：星夜绀青与暮光幽蓝深邃渐变，搭配莫兰迪薄樱色；
     * **粒子引擎**：具备 3D 俯仰翻转物理特性的樱花花瓣飘落与晨曦金粉（极简禅意留白调优）；
     * **场景美学**：和纸莳绘透光屏风模态框、短册御牒左右错落排版。
  3. **⚡ 夜之城·边缘行者 (`cyberpunk_edgerunners`)**：
     * **战术交互**：修长飘逸光刃字母 "V" 悬浮入口；来电荒坂绯红过载暴走，窃听深网赛博青雷达声呐律动；
     * **极客终端**：全屏纯文本 0-SVG 军规黑客终端控制台，支持全键盘 `1~6` / `ESC` 盲操；
     * **粒子引擎**：荧光黄/青双色高能电弧火花与垂直数据流物理引擎；
     * **场景美学**：45° 军规切角 HUD 框架、PING/NET 状态轨、脑机通信与深网潜行字幕高亮流。
  4. **🪄 死亡圣器 (`deathly_hallows`)**：
     * 优化全屏通话残留、修复小屏滚动截断与字幕同步，对齐最新模块化主题规范。
* **权威主题开发标准与沙盒生命周期**：
  * 重构维护 `frontend/js/themes/README.md` 权威开发规范，严格定义 `init`、`destroy`、`renderTrigger` 等生命周期钩子，确保主题切换时 `cancelAnimationFrame`、释放 Canvas 内存、零泄漏。

---

### 3. 🧠 深度提示词与上下文注入引擎 (Prompt & Monologue)
* **ElevenLabs V3 双 List 提示词架构**：在生成剧情时注入精准的角色列表与情绪提示，提升多角色对话一致性。
* **多轮对白与心理独白深度提取**：重构正则解析器，智能过滤思考过程与系统标签，精准提取对白并匹配情绪。
* **剧情记忆与世界观自动注入**：接入 `world_info_extractor.js`，自动提炼角色设定与世界观词条作为 LLM 剧情上下文。

---

### 4. ⚡ 全链路非阻塞异步调度与多任务队列 (Async & Queue Engine)
* **待听多任务排队队列 (`CallQueueManager`)**：
  * 后台静默生成音频，支持多个来电/窃听任务排队等待，前台阅读无任何卡顿。
  * 避免高并发下 GPT-SoVITS 显存溢出与请求拥堵。
* **播放器分段字幕同步与铭刻聊天记录**：
  * `AudioPlayer` 动态挂载与字幕逐句同步滚动高亮；
  * 全屏通话/窃听界面新增「📜 铭刻聊天 (`ChatInjector`)」按钮，一键将精彩语音剧情落盘写入 SillyTavern 主对话。

---

### 5. 📱 移动端架构模块化与高拟真交互 (Mobile UI Overhaul)
* **四大独立 App 架构拆分**：
  * `phone_call_app.js` (智能电话)、`eavesdrop_app.js` (私下窃听)、`workshop_app.js` (创作者工坊)、`theme_store_app.js` (主题商店与自定义)；
  * 通话与窃听子视图解耦：`launch_console.js`、`in_call_view.js`、`ringing_view.js`、`history_view.js`、`playback_view.js`。
* **历史记录卡片进度播放与全屏沉浸重温**：
  * 历史列表卡片内直接支持局部进度拖拽与试听；
  * 点击卡片即可一键进入全屏沉浸式通话重温模式。
* **视口与触控安全区自适应**：
  * 采用 `visualViewport` 动态计算居中，彻底消除键盘弹起或小屏滚动导致的布局错位；
  * 悬浮球采用 PointerCapture 拖拽并加入 `hasMoved` 判定，杜绝误触。

---

### 6. 🎙️ 多 TTS 供应商策略架构与系统管理升级
* **策略模式 TTS 供应商解耦**：
  * 抽象 `BaseTTSProvider` 与 `ProviderManager`，支持无缝接入云端 API；
  * 内置本地 `GPT-SoVITS` 稳定实现，并预留 `MiniMax` 与 `豆包/火山引擎` 扩展接口；
  * 酒馆原生扩展设置面板 (`frontend/settings.html`, `settings_ui.js`) 统一管理供应商与凭证。
* **Web Admin 现代化控制台重构**：
  * 采用 ES Module 模块化架构 (`admin/js/modules/`)；
  * 参考音频批量多选上传、批量删除、批量修改情绪前缀；
  * 模型批量搜索、过滤与安全删除（自动解除角色映射保护）。
* **Issue #2 彻底修复 (Remote mode HTTPS / 反向代理支持)**：
  * 智能解析远程后端地址：输入完整 `https://...` 反代地址时**绝不追加 `:3000`**；
  * 针对 `https://` 自动升级 WebSocket 为 `wss://`，杜绝 Mixed Content 拦截；
  * 针对裸 IP/host 保留指定端口或默认 3000。
* **自定义服务端口 (Manager Port)**：
  * 支持在系统配置及 `system_settings.json` 中自定义后台监听端口，并支持启动脚本自动识别。

---

## 🛠️ 关键缺陷修复与体验优化 (Bug Fixes & Refinements)

1. **[修复] Issue #2**: 修复远程模式填写完整 HTTPS 反代地址时错误拼接 `:3000` 与 `ws://` 协议的问题。
2. **[修复] 窃听/来电直达**: 修复悬浮球点击后直接重置为 home 场景的问题，修正为优先直达通话/窃听全屏接听。
3. **[修复] 音频播放器与字幕**: 修复 `AudioPlayer` 容器传参导致音频无声音/字幕未渲染的问题。
4. **[修复] DOM 状态残留**: 修复退出全屏通话后部分遮罩与粒子 Canvas 未正确销毁的问题。
5. **[优化] 极简留白与性能**: 优化三大主题粒子数量与微风间隔，CPU/GPU 占用降低 60% 以上。
6. **[优化] 数据原子读写**: `character_mappings.json` 与 `system_settings.json` 采用临时文件 rename 原子写入，杜绝进程意外关闭导致配置损坏。

---

## 🚀 升级与迁移指南 (Upgrade Guide)

1. **通过 Git 升级**：
   ```bash
   git fetch origin
   git checkout V3.0.0
   git pull origin V3.0.0
   ```
2. **重启后端服务**：
   * 双击运行 `start.bat` (Windows) 或 `./start.sh` (Linux/Mac)；
   * 后端服务将自动根据新配置完成数据库与文件系统初始化。
3. **刷新酒馆页面**：
   * 按 `Ctrl + F5` 强制刷新浏览器页面，即可体验全新 V3.0.0！
